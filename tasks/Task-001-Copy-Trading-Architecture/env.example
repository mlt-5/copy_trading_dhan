# Copy Trading System - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# REQUIRED: Account Credentials
# ============================================================================

# Leader Account (Account A - source of orders)
LEADER_CLIENT_ID=your_leader_client_id_here
LEADER_ACCESS_TOKEN=your_leader_access_token_here

# Follower Account (Account B - destination of copied orders)
FOLLOWER_CLIENT_ID=your_follower_client_id_here
FOLLOWER_ACCESS_TOKEN=your_follower_access_token_here

# ============================================================================
# OPTIONAL: System Configuration
# ============================================================================

# Environment: prod or sandbox (default: prod)
DHAN_ENV=prod

# API Base URLs (override only if needed)
# DHAN_API_BASE_URL=https://api.dhan.co
# DHAN_WS_URL=wss://api-feed.dhan.co

# ============================================================================
# Position Sizing Configuration
# ============================================================================

# Sizing Strategy: capital_proportional, fixed_ratio, or risk_based (default: capital_proportional)
SIZING_STRATEGY=capital_proportional

# Copy Ratio: Fixed multiplier for fixed_ratio strategy (e.g., 0.5 for half the size)
# COPY_RATIO=0.5

# Maximum Position Size: As percentage of available capital (default: 10.0)
MAX_POSITION_SIZE_PCT=10.0

# ============================================================================
# System Settings
# ============================================================================

# Enable/Disable Copy Trading: true or false (default: true)
ENABLE_COPY_TRADING=true

# Database Path (default: ./copy_trading.db)
SQLITE_PATH=./copy_trading.db

# Log Level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 
# 1. NEVER commit this file with real credentials to version control
# 2. Keep your access tokens secure and rotate them regularly
# 3. Use different credentials for sandbox vs production
# 4. Ensure file permissions are restrictive (chmod 600 .env)
# 5. Consider using a secret manager for production deployments
#


